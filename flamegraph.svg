<?xml version="1.0" standalone="no"?>
<!DOCTYPE svg PUBLIC "-//W3C//DTD SVG 1.1//EN" "http://www.w3.org/Graphics/SVG/1.1/DTD/svg11.dtd">
<svg version="1.1" width="1200" height="326" onload="init(evt)" viewBox="0 0 1200 326" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink">
<!-- Flame graph stack visualization. See https://github.com/brendangregg/FlameGraph for latest version, and http://www.brendangregg.com/flamegraphs.html for examples. -->
<!-- NOTES:  -->
<defs>
	<linearGradient id="background" y1="0" y2="1" x1="0" x2="0" >
		<stop stop-color="#eeeeee" offset="5%" />
		<stop stop-color="#eeeeb0" offset="95%" />
	</linearGradient>
</defs>
<style type="text/css">
	text { font-family:Verdana; font-size:12px; fill:rgb(0,0,0); }
	#search, #ignorecase { opacity:0.1; cursor:pointer; }
	#search:hover, #search.show, #ignorecase:hover, #ignorecase.show { opacity:1; }
	#subtitle { text-anchor:middle; font-color:rgb(160,160,160); }
	#title { text-anchor:middle; font-size:17px}
	#unzoom { cursor:pointer; }
	#frames > *:hover { stroke:black; stroke-width:0.5; cursor:pointer; }
	.hide { display:none; }
	.parent { opacity:0.5; }
</style>
<script type="text/ecmascript">
<![CDATA[
	"use strict";
	var details, searchbtn, unzoombtn, matchedtxt, svg, searching, currentSearchTerm, ignorecase, ignorecaseBtn;
	function init(evt) {
		details = document.getElementById("details").firstChild;
		searchbtn = document.getElementById("search");
		ignorecaseBtn = document.getElementById("ignorecase");
		unzoombtn = document.getElementById("unzoom");
		matchedtxt = document.getElementById("matched");
		svg = document.getElementsByTagName("svg")[0];
		searching = 0;
		currentSearchTerm = null;

		// use GET parameters to restore a flamegraphs state.
		var params = get_params();
		if (params.x && params.y)
			zoom(find_group(document.querySelector('[x="' + params.x + '"][y="' + params.y + '"]')));
                if (params.s) search(params.s);
	}

	// event listeners
	window.addEventListener("click", function(e) {
		var target = find_group(e.target);
		if (target) {
			if (target.nodeName == "a") {
				if (e.ctrlKey === false) return;
				e.preventDefault();
			}
			if (target.classList.contains("parent")) unzoom(true);
			zoom(target);
			if (!document.querySelector('.parent')) {
				// we have basically done a clearzoom so clear the url
				var params = get_params();
				if (params.x) delete params.x;
				if (params.y) delete params.y;
				history.replaceState(null, null, parse_params(params));
				unzoombtn.classList.add("hide");
				return;
			}

			// set parameters for zoom state
			var el = target.querySelector("rect");
			if (el && el.attributes && el.attributes.y && el.attributes._orig_x) {
				var params = get_params()
				params.x = el.attributes._orig_x.value;
				params.y = el.attributes.y.value;
				history.replaceState(null, null, parse_params(params));
			}
		}
		else if (e.target.id == "unzoom") clearzoom();
		else if (e.target.id == "search") search_prompt();
		else if (e.target.id == "ignorecase") toggle_ignorecase();
	}, false)

	// mouse-over for info
	// show
	window.addEventListener("mouseover", function(e) {
		var target = find_group(e.target);
		if (target) details.nodeValue = "Function: " + g_to_text(target);
	}, false)

	// clear
	window.addEventListener("mouseout", function(e) {
		var target = find_group(e.target);
		if (target) details.nodeValue = ' ';
	}, false)

	// ctrl-F for search
	// ctrl-I to toggle case-sensitive search
	window.addEventListener("keydown",function (e) {
		if (e.keyCode === 114 || (e.ctrlKey && e.keyCode === 70)) {
			e.preventDefault();
			search_prompt();
		}
		else if (e.ctrlKey && e.keyCode === 73) {
			e.preventDefault();
			toggle_ignorecase();
		}
	}, false)

	// functions
	function get_params() {
		var params = {};
		var paramsarr = window.location.search.substr(1).split('&');
		for (var i = 0; i < paramsarr.length; ++i) {
			var tmp = paramsarr[i].split("=");
			if (!tmp[0] || !tmp[1]) continue;
			params[tmp[0]]  = decodeURIComponent(tmp[1]);
		}
		return params;
	}
	function parse_params(params) {
		var uri = "?";
		for (var key in params) {
			uri += key + '=' + encodeURIComponent(params[key]) + '&';
		}
		if (uri.slice(-1) == "&")
			uri = uri.substring(0, uri.length - 1);
		if (uri == '?')
			uri = window.location.href.split('?')[0];
		return uri;
	}
	function find_child(node, selector) {
		var children = node.querySelectorAll(selector);
		if (children.length) return children[0];
	}
	function find_group(node) {
		var parent = node.parentElement;
		if (!parent) return;
		if (parent.id == "frames") return node;
		return find_group(parent);
	}
	function orig_save(e, attr, val) {
		if (e.attributes["_orig_" + attr] != undefined) return;
		if (e.attributes[attr] == undefined) return;
		if (val == undefined) val = e.attributes[attr].value;
		e.setAttribute("_orig_" + attr, val);
	}
	function orig_load(e, attr) {
		if (e.attributes["_orig_"+attr] == undefined) return;
		e.attributes[attr].value = e.attributes["_orig_" + attr].value;
		e.removeAttribute("_orig_"+attr);
	}
	function g_to_text(e) {
		var text = find_child(e, "title").firstChild.nodeValue;
		return (text)
	}
	function g_to_func(e) {
		var func = g_to_text(e);
		// if there's any manipulation we want to do to the function
		// name before it's searched, do it here before returning.
		return (func);
	}
	function update_text(e) {
		var r = find_child(e, "rect");
		var t = find_child(e, "text");
		var w = parseFloat(r.attributes.width.value) -3;
		var txt = find_child(e, "title").textContent.replace(/\([^(]*\)$/,"");
		t.attributes.x.value = parseFloat(r.attributes.x.value) + 3;

		// Smaller than this size won't fit anything
		if (w < 2 * 12 * 0.59) {
			t.textContent = "";
			return;
		}

		t.textContent = txt;
		var sl = t.getSubStringLength(0, txt.length);
		// check if only whitespace or if we can fit the entire string into width w
		if (/^ *$/.test(txt) || sl < w)
			return;

		// this isn't perfect, but gives a good starting point
		// and avoids calling getSubStringLength too often
		var start = Math.floor((w/sl) * txt.length);
		for (var x = start; x > 0; x = x-2) {
			if (t.getSubStringLength(0, x + 2) <= w) {
				t.textContent = txt.substring(0, x) + "..";
				return;
			}
		}
		t.textContent = "";
	}

	// zoom
	function zoom_reset(e) {
		if (e.attributes != undefined) {
			orig_load(e, "x");
			orig_load(e, "width");
		}
		if (e.childNodes == undefined) return;
		for (var i = 0, c = e.childNodes; i < c.length; i++) {
			zoom_reset(c[i]);
		}
	}
	function zoom_child(e, x, ratio) {
		if (e.attributes != undefined) {
			if (e.attributes.x != undefined) {
				orig_save(e, "x");
				e.attributes.x.value = (parseFloat(e.attributes.x.value) - x - 10) * ratio + 10;
				if (e.tagName == "text")
					e.attributes.x.value = find_child(e.parentNode, "rect[x]").attributes.x.value + 3;
			}
			if (e.attributes.width != undefined) {
				orig_save(e, "width");
				e.attributes.width.value = parseFloat(e.attributes.width.value) * ratio;
			}
		}

		if (e.childNodes == undefined) return;
		for (var i = 0, c = e.childNodes; i < c.length; i++) {
			zoom_child(c[i], x - 10, ratio);
		}
	}
	function zoom_parent(e) {
		if (e.attributes) {
			if (e.attributes.x != undefined) {
				orig_save(e, "x");
				e.attributes.x.value = 10;
			}
			if (e.attributes.width != undefined) {
				orig_save(e, "width");
				e.attributes.width.value = parseInt(svg.width.baseVal.value) - (10 * 2);
			}
		}
		if (e.childNodes == undefined) return;
		for (var i = 0, c = e.childNodes; i < c.length; i++) {
			zoom_parent(c[i]);
		}
	}
	function zoom(node) {
		var attr = find_child(node, "rect").attributes;
		var width = parseFloat(attr.width.value);
		var xmin = parseFloat(attr.x.value);
		var xmax = parseFloat(xmin + width);
		var ymin = parseFloat(attr.y.value);
		var ratio = (svg.width.baseVal.value - 2 * 10) / width;

		// XXX: Workaround for JavaScript float issues (fix me)
		var fudge = 0.0001;

		unzoombtn.classList.remove("hide");

		var el = document.getElementById("frames").children;
		for (var i = 0; i < el.length; i++) {
			var e = el[i];
			var a = find_child(e, "rect").attributes;
			var ex = parseFloat(a.x.value);
			var ew = parseFloat(a.width.value);
			var upstack;
			// Is it an ancestor
			if (0 == 0) {
				upstack = parseFloat(a.y.value) > ymin;
			} else {
				upstack = parseFloat(a.y.value) < ymin;
			}
			if (upstack) {
				// Direct ancestor
				if (ex <= xmin && (ex+ew+fudge) >= xmax) {
					e.classList.add("parent");
					zoom_parent(e);
					update_text(e);
				}
				// not in current path
				else
					e.classList.add("hide");
			}
			// Children maybe
			else {
				// no common path
				if (ex < xmin || ex + fudge >= xmax) {
					e.classList.add("hide");
				}
				else {
					zoom_child(e, xmin, ratio);
					update_text(e);
				}
			}
		}
		search();
	}
	function unzoom(dont_update_text) {
		unzoombtn.classList.add("hide");
		var el = document.getElementById("frames").children;
		for(var i = 0; i < el.length; i++) {
			el[i].classList.remove("parent");
			el[i].classList.remove("hide");
			zoom_reset(el[i]);
			if(!dont_update_text) update_text(el[i]);
		}
		search();
	}
	function clearzoom() {
		unzoom();

		// remove zoom state
		var params = get_params();
		if (params.x) delete params.x;
		if (params.y) delete params.y;
		history.replaceState(null, null, parse_params(params));
	}

	// search
	function toggle_ignorecase() {
		ignorecase = !ignorecase;
		if (ignorecase) {
			ignorecaseBtn.classList.add("show");
		} else {
			ignorecaseBtn.classList.remove("show");
		}
		reset_search();
		search();
	}
	function reset_search() {
		var el = document.querySelectorAll("#frames rect");
		for (var i = 0; i < el.length; i++) {
			orig_load(el[i], "fill")
		}
		var params = get_params();
		delete params.s;
		history.replaceState(null, null, parse_params(params));
	}
	function search_prompt() {
		if (!searching) {
			var term = prompt("Enter a search term (regexp " +
			    "allowed, eg: ^ext4_)"
			    + (ignorecase ? ", ignoring case" : "")
			    + "\nPress Ctrl-i to toggle case sensitivity", "");
			if (term != null) search(term);
		} else {
			reset_search();
			searching = 0;
			currentSearchTerm = null;
			searchbtn.classList.remove("show");
			searchbtn.firstChild.nodeValue = "Search"
			matchedtxt.classList.add("hide");
			matchedtxt.firstChild.nodeValue = ""
		}
	}
	function search(term) {
		if (term) currentSearchTerm = term;
		if (currentSearchTerm === null) return;

		var re = new RegExp(currentSearchTerm, ignorecase ? 'i' : '');
		var el = document.getElementById("frames").children;
		var matches = new Object();
		var maxwidth = 0;
		for (var i = 0; i < el.length; i++) {
			var e = el[i];
			var func = g_to_func(e);
			var rect = find_child(e, "rect");
			if (func == null || rect == null)
				continue;

			// Save max width. Only works as we have a root frame
			var w = parseFloat(rect.attributes.width.value);
			if (w > maxwidth)
				maxwidth = w;

			if (func.match(re)) {
				// highlight
				var x = parseFloat(rect.attributes.x.value);
				orig_save(rect, "fill");
				rect.attributes.fill.value = "rgb(230,0,230)";

				// remember matches
				if (matches[x] == undefined) {
					matches[x] = w;
				} else {
					if (w > matches[x]) {
						// overwrite with parent
						matches[x] = w;
					}
				}
				searching = 1;
			}
		}
		if (!searching)
			return;
		var params = get_params();
		params.s = currentSearchTerm;
		history.replaceState(null, null, parse_params(params));

		searchbtn.classList.add("show");
		searchbtn.firstChild.nodeValue = "Reset Search";

		// calculate percent matched, excluding vertical overlap
		var count = 0;
		var lastx = -1;
		var lastw = 0;
		var keys = Array();
		for (k in matches) {
			if (matches.hasOwnProperty(k))
				keys.push(k);
		}
		// sort the matched frames by their x location
		// ascending, then width descending
		keys.sort(function(a, b){
			return a - b;
		});
		// Step through frames saving only the biggest bottom-up frames
		// thanks to the sort order. This relies on the tree property
		// where children are always smaller than their parents.
		var fudge = 0.0001;	// JavaScript floating point
		for (var k in keys) {
			var x = parseFloat(keys[k]);
			var w = matches[keys[k]];
			if (x >= lastx + lastw - fudge) {
				count += w;
				lastx = x;
				lastw = w;
			}
		}
		// display matched percent
		matchedtxt.classList.remove("hide");
		var pct = 100 * count / maxwidth;
		if (pct != 100) pct = pct.toFixed(1)
		matchedtxt.firstChild.nodeValue = "Matched: " + pct + "%";
	}
]]>
</script>
<rect x="0.0" y="0" width="1200.0" height="326.0" fill="url(#background)"  />
<text id="title" x="600.00" y="24" >Flame Graph</text>
<text id="details" x="10.00" y="309" > </text>
<text id="unzoom" x="10.00" y="24" class="hide">Reset Zoom</text>
<text id="search" x="1090.00" y="24" >Search</text>
<text id="ignorecase" x="1174.00" y="24" >ic</text>
<text id="matched" x="1090.00" y="309" > </text>
<g id="frames">
<g >
<title>std::operator&amp; (30,303,030 samples, 1.04%)</title><rect x="206.7" y="69" width="12.3" height="15.0" fill="rgb(221,76,18)" rx="2" ry="2" />
<text  x="209.67" y="79.5" ></text>
</g>
<g >
<title>std::__atomic_base&lt;unsigned long&gt;::operator+= (434,343,430 samples, 14.93%)</title><rect x="1013.8" y="101" width="176.2" height="15.0" fill="rgb(237,149,35)" rx="2" ry="2" />
<text  x="1016.82" y="111.5" >std::__atomic_base&lt;uns..</text>
</g>
<g >
<title>irqentry_exit_to_user_mode (10,101,010 samples, 0.35%)</title><rect x="1009.7" y="53" width="4.1" height="15.0" fill="rgb(254,229,54)" rx="2" ry="2" />
<text  x="1012.72" y="63.5" ></text>
</g>
<g >
<title>std::operator&amp; (40,404,040 samples, 1.39%)</title><rect x="784.4" y="69" width="16.4" height="15.0" fill="rgb(221,76,18)" rx="2" ry="2" />
<text  x="787.38" y="79.5" ></text>
</g>
<g >
<title>std::__atomic_base&lt;unsigned long&gt;::load (80,808,080 samples, 2.78%)</title><rect x="395.1" y="101" width="32.8" height="15.0" fill="rgb(230,118,28)" rx="2" ry="2" />
<text  x="398.14" y="111.5" >st..</text>
</g>
<g >
<title>__memmove_avx_unaligned_erms (50,505,050 samples, 1.74%)</title><rect x="702.4" y="85" width="20.5" height="15.0" fill="rgb(205,0,0)" rx="2" ry="2" />
<text  x="705.43" y="95.5" ></text>
</g>
<g >
<title>std::__atomic_base&lt;unsigned long&gt;::load (191,919,190 samples, 6.60%)</title><rect x="722.9" y="85" width="77.9" height="15.0" fill="rgb(230,118,28)" rx="2" ry="2" />
<text  x="725.92" y="95.5" >std::__a..</text>
</g>
<g >
<title>__memmove_avx_unaligned_erms (141,414,140 samples, 4.86%)</title><rect x="100.1" y="85" width="57.4" height="15.0" fill="rgb(205,0,0)" rx="2" ry="2" />
<text  x="103.14" y="95.5" >__memm..</text>
</g>
<g >
<title>irqentry_exit (10,101,010 samples, 0.35%)</title><rect x="1009.7" y="69" width="4.1" height="15.0" fill="rgb(219,66,15)" rx="2" ry="2" />
<text  x="1012.72" y="79.5" ></text>
</g>
<g >
<title>std::__atomic_base&lt;unsigned long&gt;::store (515,151,510 samples, 17.71%)</title><rect x="800.8" y="85" width="208.9" height="15.0" fill="rgb(219,68,16)" rx="2" ry="2" />
<text  x="803.76" y="95.5" >std::__atomic_base&lt;unsigned..</text>
</g>
<g >
<title>std::operator&amp; (20,202,020 samples, 0.69%)</title><rect x="10.0" y="213" width="8.2" height="15.0" fill="rgb(221,76,18)" rx="2" ry="2" />
<text  x="13.00" y="223.5" ></text>
</g>
<g >
<title>std::thread::_Invoker&lt;std::tuple&lt;void  (2,888,888,860 samples, 99.31%)</title><rect x="18.2" y="181" width="1171.8" height="15.0" fill="rgb(236,146,35)" rx="2" ry="2" />
<text  x="21.19" y="191.5" >std::thread::_Invoker&lt;std::tuple&lt;void </text>
</g>
<g >
<title>Consumer (1,242,424,230 samples, 42.71%)</title><rect x="18.2" y="117" width="504.0" height="15.0" fill="rgb(211,29,7)" rx="2" ry="2" />
<text  x="21.19" y="127.5" >Consumer</text>
</g>
<g >
<title>std::__invoke_impl&lt;void, void  (2,888,888,860 samples, 99.31%)</title><rect x="18.2" y="133" width="1171.8" height="15.0" fill="rgb(227,101,24)" rx="2" ry="2" />
<text  x="21.19" y="143.5" >std::__invoke_impl&lt;void, void </text>
</g>
<g >
<title>std::__atomic_base&lt;unsigned long&gt;::load (20,202,020 samples, 0.69%)</title><rect x="10.0" y="229" width="8.2" height="15.0" fill="rgb(230,118,28)" rx="2" ry="2" />
<text  x="13.00" y="239.5" ></text>
</g>
<g >
<title>std::__atomic_base&lt;unsigned long&gt;::load (151,515,150 samples, 5.21%)</title><rect x="157.5" y="85" width="61.5" height="15.0" fill="rgb(230,118,28)" rx="2" ry="2" />
<text  x="160.50" y="95.5" >std::_..</text>
</g>
<g >
<title>all (2,909,090,880 samples, 100%)</title><rect x="10.0" y="277" width="1180.0" height="15.0" fill="rgb(213,39,9)" rx="2" ry="2" />
<text  x="13.00" y="287.5" ></text>
</g>
<g >
<title>start_thread (2,888,888,860 samples, 99.31%)</title><rect x="18.2" y="229" width="1171.8" height="15.0" fill="rgb(212,34,8)" rx="2" ry="2" />
<text  x="21.19" y="239.5" >start_thread</text>
</g>
<g >
<title>CRingBuff::GetData (898,989,890 samples, 30.90%)</title><rect x="30.5" y="101" width="364.6" height="15.0" fill="rgb(245,184,44)" rx="2" ry="2" />
<text  x="33.49" y="111.5" >CRingBuff::GetData</text>
</g>
<g >
<title>std::__atomic_base&lt;unsigned long&gt;::operator+= (232,323,230 samples, 7.99%)</title><rect x="427.9" y="101" width="94.3" height="15.0" fill="rgb(237,149,35)" rx="2" ry="2" />
<text  x="430.92" y="111.5" >std::__atom..</text>
</g>
<g >
<title>__GI___clone (2,888,888,860 samples, 99.31%)</title><rect x="18.2" y="245" width="1171.8" height="15.0" fill="rgb(247,195,46)" rx="2" ry="2" />
<text  x="21.19" y="255.5" >__GI___clone</text>
</g>
<g >
<title>Consumer (20,202,020 samples, 0.69%)</title><rect x="10.0" y="245" width="8.2" height="15.0" fill="rgb(211,29,7)" rx="2" ry="2" />
<text  x="13.00" y="255.5" ></text>
</g>
<g >
<title>sysvec_call_function_single (10,101,010 samples, 0.35%)</title><rect x="1009.7" y="85" width="4.1" height="15.0" fill="rgb(221,78,18)" rx="2" ry="2" />
<text  x="1012.72" y="95.5" ></text>
</g>
<g >
<title>asm_sysvec_call_function_single (10,101,010 samples, 0.35%)</title><rect x="1009.7" y="101" width="4.1" height="15.0" fill="rgb(234,136,32)" rx="2" ry="2" />
<text  x="1012.72" y="111.5" ></text>
</g>
<g >
<title>Producer (1,646,464,630 samples, 56.60%)</title><rect x="522.2" y="117" width="667.8" height="15.0" fill="rgb(213,37,8)" rx="2" ry="2" />
<text  x="525.15" y="127.5" >Producer</text>
</g>
<g >
<title>main (2,909,090,880 samples, 100.00%)</title><rect x="10.0" y="261" width="1180.0" height="15.0" fill="rgb(243,179,42)" rx="2" ry="2" />
<text  x="13.00" y="271.5" >main</text>
</g>
<g >
<title>[libstdc++.so.6.0.28] (2,888,888,860 samples, 99.31%)</title><rect x="18.2" y="213" width="1171.8" height="15.0" fill="rgb(240,162,38)" rx="2" ry="2" />
<text  x="21.19" y="223.5" >[libstdc++.so.6.0.28]</text>
</g>
<g >
<title>std::operator&amp; (20,202,020 samples, 0.69%)</title><rect x="1001.5" y="69" width="8.2" height="15.0" fill="rgb(221,76,18)" rx="2" ry="2" />
<text  x="1004.53" y="79.5" ></text>
</g>
<g >
<title>std::operator&amp; (10,101,010 samples, 0.35%)</title><rect x="423.8" y="85" width="4.1" height="15.0" fill="rgb(221,76,18)" rx="2" ry="2" />
<text  x="426.82" y="95.5" ></text>
</g>
<g >
<title>std::__atomic_base&lt;unsigned long&gt;::store (363,636,360 samples, 12.50%)</title><rect x="219.0" y="85" width="147.5" height="15.0" fill="rgb(219,68,16)" rx="2" ry="2" />
<text  x="221.96" y="95.5" >std::__atomic_base..</text>
</g>
<g >
<title>CRingBuff::PutData (1,151,515,140 samples, 39.58%)</title><rect x="542.6" y="101" width="467.1" height="15.0" fill="rgb(233,131,31)" rx="2" ry="2" />
<text  x="545.64" y="111.5" >CRingBuff::PutData</text>
</g>
<g >
<title>std::operator&amp; (10,101,010 samples, 0.35%)</title><rect x="362.4" y="69" width="4.1" height="15.0" fill="rgb(221,76,18)" rx="2" ry="2" />
<text  x="365.36" y="79.5" ></text>
</g>
<g >
<title>std::__invoke&lt;void  (2,888,888,860 samples, 99.31%)</title><rect x="18.2" y="149" width="1171.8" height="15.0" fill="rgb(238,155,37)" rx="2" ry="2" />
<text  x="21.19" y="159.5" >std::__invoke&lt;void </text>
</g>
<g >
<title>exit_to_user_mode_prepare (10,101,010 samples, 0.35%)</title><rect x="1009.7" y="37" width="4.1" height="15.0" fill="rgb(228,108,25)" rx="2" ry="2" />
<text  x="1012.72" y="47.5" ></text>
</g>
<g >
<title>std::thread::_State_impl&lt;std::thread::_Invoker&lt;std::tuple&lt;void  (2,888,888,860 samples, 99.31%)</title><rect x="18.2" y="197" width="1171.8" height="15.0" fill="rgb(234,137,32)" rx="2" ry="2" />
<text  x="21.19" y="207.5" >std::thread::_State_impl&lt;std::thread::_Invoker&lt;std::tuple&lt;void </text>
</g>
<g >
<title>std::min&lt;unsigned long&gt; (70,707,070 samples, 2.43%)</title><rect x="366.5" y="85" width="28.6" height="15.0" fill="rgb(235,140,33)" rx="2" ry="2" />
<text  x="369.46" y="95.5" >st..</text>
</g>
<g >
<title>std::thread::_Invoker&lt;std::tuple&lt;void  (2,888,888,860 samples, 99.31%)</title><rect x="18.2" y="165" width="1171.8" height="15.0" fill="rgb(236,146,35)" rx="2" ry="2" />
<text  x="21.19" y="175.5" >std::thread::_Invoker&lt;std::tuple&lt;void </text>
</g>
</g>
</svg>
